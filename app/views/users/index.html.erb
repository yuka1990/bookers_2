<%= render 'books/common', book: @book %>







<h1>Users</h1>
<h2>image</h2>
<h2>name</h2>
<% @users.each do |user| %>
 
   <%= image_tag user.get_profile_image(100,100) %>
   <%= user.name %>
   <%= link_to "show",user_path(user.id) %>
<% end %>